<%= form_with(model: [:control_panel, product_segment], local: true) do |form| %>
  <% if product_segment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_segment.errors.count, "error") %> prohibited this product_segment from being saved:</h2>

      <ul>
      <% product_segment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :product_segment_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
