<%= form_with(model: [:control_panel, product_property], local: true) do |form| %>
  <% if product_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_property.errors.count, "error") %> prohibited this product_property from being saved:</h2>

      <ul>
      <% product_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :product_property_product_id %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size, id: :product_property_size %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :product_property_color %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :product_property_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
